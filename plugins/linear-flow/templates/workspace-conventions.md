# Linear workspace conventions

> Copy to `~/.claude/linear-conventions.md` and fill in. Every section is optional — delete what does not apply rather than leaving a placeholder, because an unfilled heading reads as a declared rule. What is written here wins over the plugin's generic defaults; what is left out falls back to them.

## Team and identity

Default team: `<team name or key>`. <Note here if more than one team is in play and how to choose.>

## Project vs issue

Create a **project** when: <the rule — kinds of work that deserve their own scope>.

Create an **issue** when: <the rule — single execution items>.

When ambiguous: <which way to lean, or "ask">.

## Naming patterns

- Client work: `<pattern>`
- Internal feature or initiative: `<pattern>`
- Discovery: `<pattern>`
- Issues: `<pattern, or "concise and direct, no fixed form">`

## Description shapes

- Project: <sections, in order>
- Discovery: <sections, in order>
- Issue: <sections, or "practical and low-bloat, no fixed shape">

## Workflow statuses

In order: `<status>` → `<status>` → `<status>`

Meanings that the name does not carry on its own:

- `<status>` — <what it actually signals>

## Label taxonomy

Issue labels:

- `<group>` — `<value>`, `<value>`. Applies when <condition>.

Project labels:

- `<group>` — `<value>`, `<value>`. Applies when <condition>.

## Templates

- `<template name>` — for <kind of work>.

## Views

- `<view name>` — <what it holds and when it is worth reading>.

## Working style

Cycles: <on / off>. Estimates: <on / off, and what the unit means>.

<Anything else about how much process this workspace wants.>
