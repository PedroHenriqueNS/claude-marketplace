{
  "skill_name": "ln-ship-loop",
  "evals": [
    {
      "id": 1,
      "name": "open-pr-attaches-and-proposes-status",
      "prompt": "I just opened the PR for this branch — sync Linear",
      "expected_output": "Should read the current branch, the commits not on the main branch, and the PR via `gh pr view`; find the issue by an identifier in the branch/commits/PR title and show the match for confirmation; attach the PR URL as a link (not the diff or a generated summary); then propose the status move that matches an OPEN PR using the workspace's own status meanings, showing current -> proposed with its evidence, and apply it only after approval. Must NOT commit, push, or open anything, and must NOT move status on inference alone.",
      "files": []
    },
    {
      "id": 2,
      "name": "no-matching-issue-offers-to-create-an-issue",
      "prompt": "Link this branch to Linear",
      "expected_output": "When no identifier appears in the branch, commits, or PR title and a text search finds no plausible match, should say so and offer to create ONE issue — not a project, because a branch is an execution item — drafted from the commits and PR, shown before writing. If a text search returns two plausible matches, should ask rather than ranking them. A branch with commits and no PR is a normal state to report, not a failure, and no PR URL may be invented.",
      "files": []
    },
    {
      "id": 3,
      "name": "negative-trigger-pure-git-request",
      "prompt": "Commit this and push it up",
      "expected_output": "Should NOT trigger this skill — there is no Linear intent, and this skill never commits, pushes, or opens PRs. The commit-commands plugin owns this.",
      "files": []
    },
    {
      "id": 4,
      "name": "asks-pr-relation-before-attaching-and-caps-status",
      "prompt": "PR is up for this branch — attach it to PED-7 and update the status",
      "expected_output": "Before attaching anything, should ask what the PR is to PED-7 — resolves it, contributes to it, or is merely related to it — showing both the PR title and PED-7's own title so a mis-matched issue is visible before the write, and marking a recommended answer ONLY when the branch or PR title carries the identifier AND the PR covers the whole of what the issue asks for. Should title the link with that answer (e.g. \"Contributes to · <PR title>\"), then bound the status proposal by it: a contributing PR may reach a started status but never a completed one, and a merely related PR justifies no move at all. Must still show current -> proposed with its evidence and move only on approval. Must NOT edit the PR description to add a magic word, and must NOT treat \"resolves\" as permission to move without approval.",
      "files": []
    }
  ]
}
