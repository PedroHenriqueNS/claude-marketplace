---
description: The bare phrase "groom the backlog" about a local Markdown file, with no Linear context. No ln-* skill may fire.
tags: [negative]
max_turns: 10
timeout_seconds: 300
allowed_tools: [Skill, Read, Glob, Grep]
---

Our backlog lives in BACKLOG.md at the repo root. Groom the backlog: merge duplicate items and move anything older than a quarter into an Icebox section.
