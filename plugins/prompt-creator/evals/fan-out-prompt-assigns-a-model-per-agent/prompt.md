---
description: The rewrite dispatches subagents, so it must name a model per agent with one clause of why, and name silent inheritance as the defect fixed.
tags: [online]
max_turns: 20
timeout_seconds: 600
allowed_tools: [Skill, Read, Glob, Grep, WebFetch]
---

Improve this prompt: "use subagents to check all 40 packages under packages/ for unused dependencies, then have another agent write up the findings in a report"
