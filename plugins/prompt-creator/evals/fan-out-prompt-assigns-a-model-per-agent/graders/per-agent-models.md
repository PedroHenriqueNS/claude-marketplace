---
type: llm
---

PASS if all of these hold: (1) the rewritten prompt names a model for the agents that scan the 40 packages and a different, higher-tier model for the agent that writes the report; (2) each assignment carries a short reason, such as mechanical breadth for the scans and judgment for the report; (3) the explanation of what changed says that, unassigned, every agent would have inherited the session's model.

FAIL if every agent gets the same model, no model is named, or a choice is justified with benchmark numbers or measured cost or accuracy figures.
